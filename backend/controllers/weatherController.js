import { geocode } from '../services/osmService.js';
export const getWeather = async (req,res)=>{try{const destination=(req.query.destination||'').trim();if(!destination)return res.status(400).json({success:false,message:'Destination is required'});const geo=await geocode(destination);const url=`https://api.open-meteo.com/v1/forecast?latitude=${geo.latitude}&longitude=${geo.longitude}&current=temperature_2m,relative_humidity_2m,apparent_temperature,weather_code,wind_speed_10m&daily=weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max&timezone=auto&forecast_days=16`;const r=await fetch(url);if(!r.ok)throw new Error('Weather service unavailable');const weather=await r.json();res.json({success:true,destination:geo,weather});}catch(e){res.status(502).json({success:false,message:e.message||'Failed to get weather'})}};
